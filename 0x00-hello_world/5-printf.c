#include <stdio.h>

/**
 * main - Entry point
 *
 * Print with printf()
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* Initialize strings */
	char str[] = "with proper grammar, but the outcome is a piece of art,";

	/* Writing to stdout */
	printf("%s\n", str);

	return (0);
}
